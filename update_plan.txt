1. Update `StudentDashboard.tsx` to conditionally hide the bottom navigation bar on more full-screen views, including `WEEKLY_TEST`, `CUSTOM_PAGE`, `CHALLENGE_20`, and `AI_STUDIO`.
2. Ensure that any screen that requires full viewport (like an MCQ test or custom full-screen flow) completely hides the `fixed bottom-0` element.
